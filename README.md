# HTTP-Web-Server
A programming assignment for my computer networks class

The server must be running at all times in order to use. Only run the client after the server is up and running.

To test the OK 200 code, leave the code as is and run it. To test the 404 Error code, switch the code on line 15 of Zeldin_WebClient.py to client_socket.send(badRequest.encode()).
